name: password_generator
description: Random password generator for Windows.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  msix: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

msix_config:
  display_name: Password Generator
  publisher_display_name: flutter.dev
  msix_version: 1.0.0.0
  logo_path: C:\Users\Daniel\Desktop\Development\flutter_win_password_generator\icon.png
  icons_background_color: "#ffffff"
  architecture: x64
